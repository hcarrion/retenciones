import { BuscarListaComando } from './buscar-lista-comando';

describe('BuscarListaComando', () => {
  it('should create an instance', () => {
    expect(new BuscarListaComando()).toBeTruthy();
  });
});
