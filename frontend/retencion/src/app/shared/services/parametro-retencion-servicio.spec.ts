import { ParametroRetencionServicio } from './parametro-retencion-servicio';

describe('ParametroRetencionServicio', () => {
  it('should create an instance', () => {
    expect(new ParametroRetencionServicio()).toBeTruthy();
  });
});
