import { TraedatabaseService } from './traedatabase-service';

describe('TraedatabaseService', () => {
  it('should create an instance', () => {
    expect(new TraedatabaseService()).toBeTruthy();
  });
});
