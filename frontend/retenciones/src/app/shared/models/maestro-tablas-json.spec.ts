import { MaestroTablasJson } from './maestro-tablas-json';

describe('MaestroTablasJson', () => {
  it('should create an instance', () => {
    expect(new MaestroTablasJson()).toBeTruthy();
  });
});
