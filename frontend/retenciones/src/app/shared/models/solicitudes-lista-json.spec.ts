import { SolicitudesListaJson } from './solicitudes-lista-json';

describe('SolicitudesListaJson', () => {
  it('should create an instance', () => {
    expect(new SolicitudesListaJson()).toBeTruthy();
  });
});
